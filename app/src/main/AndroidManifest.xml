<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TalkBackTips"
        tools:targetApi="31">
        <activity
            android:name=".CustomActionsActivity"
            android:exported="false"
            android:label="@string/title_activity_custom_actions"
            android:theme="@style/Theme.TalkBackTips" />
        <activity
            android:name=".StateChangeActivity"
            android:exported="false"
            android:label="@string/title_activity_state_change"
            android:theme="@style/Theme.TalkBackTips" />
        <activity
            android:name=".GroupingActivity"
            android:exported="false"
            android:label="@string/title_activity_grouping"
            android:theme="@style/Theme.TalkBackTips" />
        <activity
            android:name=".TextFieldActivity"
            android:exported="false"
            android:label="@string/title_activity_text_field"
            android:theme="@style/Theme.TalkBackTips" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TalkBackTips">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>